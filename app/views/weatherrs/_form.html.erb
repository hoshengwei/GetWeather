<%= form_for(weatherr) do |f| %>
  <% if weatherr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weatherr.errors.count, "error") %> prohibited this weatherr from being saved:</h2>

      <ul>
      <% weatherr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
